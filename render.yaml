services:
  - type: web
    name: maire-pood-backend
    runtime: docker
    plan: free
    envVars:
      - key: ADMIN_PASSWORD
        value: maire2026
      - key: DATABASE_URL
        fromDatabase:
          name: maire-pood-db
          property: connectionString

databases:
  - name: maire-pood-db
    databaseName: mairepood
    user: mairepood
